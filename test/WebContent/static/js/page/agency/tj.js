$(document).ready(function() {
	//$.get("../../report/findUserList",function(data){
		  
	//	   $('#result').text(data);//直接展示JSON数据
		  // fuzhi(data);
		   
	//	   });
});

function fuzhi(data){
	  // zNodes=eval("("+data+")");
	  // $.fn.zTree.init($("#treeDemo"), setting, zNodes);
}
